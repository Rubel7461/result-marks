let names = prompt(`Type your Names`);
let amount = prompt(`Hello, ${names}, Type your Amount`);
let type = prompt(`Hello, ${names}, Type your Currency Type(Now available USD, CAD, Euro, Pound)`)

converter(amount,type);